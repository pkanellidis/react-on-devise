<%= react_component("Register", props: {
  registrationPath: registration_path(resource_name),
  navigationPaths: navigation_paths_for_scope,
  csrfToken: form_authenticity_token,
  minimumPasswordLength: @minimum_password_length,
}) %>